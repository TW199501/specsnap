# SpecSnap v0.0.4 + v0.0.5 Closeout Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship two consecutive patch releases today — `0.0.4` (disk-landing story: File System Access API + polish) then `0.0.5` (schema depth: `data-i18n-key` + `data-v-source` reverse lookup + publish automation workflow) — and close out the 0.0.x pre-alpha chapter with a retrospective.

**Architecture:** v0.0.4 adds a coordinate-agnostic `fs-access` adapter module to the playground that prefers the File System Access API (Chrome/Edge 86+) and cleanly falls back to the existing `<a download>` flow on older browsers. v0.0.5 extends `ElementIdentity` with two optional fields (`i18nKey`, `source`) that are populated only when the consumer's build-time tooling has injected the corresponding `data-*` attributes; core stays agnostic to any specific i18n library or bundler. Both releases keep `Session`/`Frame`/`Gap` wire-compatible for pre-0.0.3 consumers — new fields are additive and `SCHEMA_VERSION` bumps to `'0.0.5'` only at the final release.

**Tech Stack:** TypeScript strict, vitest (happy-dom), tsup, pnpm workspace, GitHub Actions, File System Access API (progressive enhancement), IndexedDB (for folder handle persistence).

**Scope note:** This plan has two sub-projects (filesystem UX vs schema fields). They're kept in one document because the user's explicit goal is "finish today" and the two sub-projects share the release-gate workflow (same `pnpm check && pnpm test && pnpm build`, same publish ritual). Each sub-project produces working software on its own.

---

## File Structure

**New files:**

*   `apps/playground/fs-access.ts` — File System Access API adapter with `<a download>` fallback. Exports `writeBundle(bundle) → Promise<WriteResult>` + `setupWriteTarget() → Promise<WriteTarget>`.
*   `apps/playground/fs-access.test.ts` — unit tests for the **pure logic only** (feature detection, fallback routing). FSA and IndexedDB are browser-only — verify end-to-end via Playwright.
*   `.github/workflows/publish.yml` — tag-triggered npm publish + GitHub Release. Needs `NPM_TOKEN` secret (user sets up later).
*   `docs/superpower/plan/2026-04-20-retrospective-v003-to-v005.md` — closeout retrospective documenting the 0.0.x chapter.

**Modified files:**

*   `packages/core/src/types.ts` — add `ElementIdentity.i18nKey?: string` + `ElementIdentity.source?: string` (v0.0.5).
*   `packages/core/src/capture.ts` — read `data-i18n-key` and `data-v-source` attributes in `identify()` (v0.0.5).
*   `packages/core/src/serialize-md.ts` — emit new fields in the "基本 (Basics)" section when present.
*   `packages/core/src/capture.test.ts` — new tests for the two attributes (v0.0.5).
*   `packages/core/src/serialize-md.test.ts` — new tests for the MD emission (v0.0.5).
*   `packages/core/package.json` — version bumps 0.0.3 → 0.0.4 → 0.0.5.
*   `packages/core/README.md` — document new identity fields + FSA note (v0.0.5).
*   `apps/playground/main.ts` — delegate writes to `fs-access.writeBundle()` instead of directly calling `triggerDownload` (v0.0.4).
*   `apps/playground/index.html` — add "Choose folder" button + UI hint about filesystem mode (v0.0.4).
*   `README.md` (root) — update to reflect 0.0.3 reality, then 0.0.4/0.0.5 at each release.
*   `packages/core/src/capture.ts` — border subpixel display polish helper (v0.0.4).
*   `packages/core/src/serialize-md.ts` — use the display helper for border values (v0.0.4).

**Not touched:**

*   `packages/core/src/to-annotated-png.ts`, `to-specsnap-bundle.ts`, `annotate.ts`, `gap.ts`, `viewport.ts`, `lexicon.ts` — frozen.

---

# PART 1 — v0.0.4 (Disk landing + polish)

### Task 1: Root README refresh to v0.0.3 reality

Context: the GitHub repo was just made public. The root README still reflects v0.0.2 ("v0.0.2 · inter-element gap distances"). Outside visitors see outdated info.

**Files:**

Modify: `README.md` (repo root)

 **Step 1: Read the current README**

Run: `cat README.md`

*   **Step 2: Update the Packages table version column**

In `README.md`, find the line:

```
| [`@tw199501/specsnap-core`](./packages/core) | ✅ 0.0.2 | TypeScript library: capture + serialize (MD / JSON) |
```

Replace with:

```
| [`@tw199501/specsnap-core`](./packages/core) | ✅ 0.0.3 | TypeScript library: capture + serialize (MD / JSON) + annotated PNG + disk-ready bundles |
```

*   **Step 3: Update Status section**

In `README.md`, find the line:

```
🚧 **Pre-alpha (v0.0.x)** — schema may change. Locking in at v1.0.
```

Add a "What v0.0.3 brought" subsection immediately after:

```
🚧 **Pre-alpha (v0.0.x)** — schema may change. Locking in at v1.0.

### What v0.0.3 brought

- **`toAnnotatedPNG`** — one annotated PNG per frame, focus-frame isolation
- **`toSpecSnapBundle`** — disk-ready bundle: MD + PNGs named `YYYYMMDD-NN-*.png` with relative-path refs inside the MD
- **`filter` option on capture** — exclude consumer UI chrome (panels, toolbars) from the screenshot
```

*   **Step 4: LF check + commit**

Run: `pnpm check:eol`
Expected: `All N tracked text files use LF.`

```
git add README.md
git commit -m "docs: root README reflects v0.0.3 shipped features"
```

---

### Task 2: Border subpixel display polish

Context: CSS `border: 1px` on a DPR 1.5 display is reported as `0.67` by `getComputedStyle`. Technically correct but reads weird in the MD. Fix: show the rounded integer in MD when the original value would round to 1px, but keep the original precision in JSON for programmatic consumers.

**Files:**

Modify: `packages/core/src/serialize-md.ts`

Modify: `packages/core/src/serialize-md.test.ts`

 **Step 1: Write a failing test for border display**

In `packages/core/src/serialize-md.test.ts`, append:

```
describe('border subpixel display', () => {
  it('rounds fractional border widths to the nearest CSS pixel in MD output', () => {
    // Synthesize a session manually to control the border values.
    const session = {
      schemaVersion: '0.0.2' as const,
      id: 's-test',
      capturedAt: '2026-04-20T00:00:00Z',
      url: '',
      pageTitle: '',
      viewport: { width: 1000, height: 800, devicePixelRatio: 1.5 },
      scroll: { x: 0, y: 0 },
      frames: [{
        index: 1,
        identity: {
          tagName: 'input',
          id: 'username',
          classList: [],
          name: 'input#username',
          domPath: 'input#username'
        },
        rect: { x: 0, y: 0, width: 245, height: 43 },
        viewportRelative: { xPct: 0, yPct: 0 },
        boxModel: {
          content: { width: 219, height: 33 },
          padding: [4, 12, 4, 12] as const,
          border: [0.67, 0.67, 0.67, 0.67] as const,
          margin: [0, 0, 0, 0] as const
        },
        typography: {
          fontFamily: 'Arial', fontSize: 13, fontWeight: '400',
          lineHeight: 'normal', letterSpacing: 'normal',
          color: 'rgb(0,0,0)', textAlign: 'start'
        },
        background: {
          color: 'rgb(255,255,255)', image: 'none',
          borderRadius: [6, 6, 6, 6] as const
        }
      }],
      gaps: []
    };
    const md = toMarkdown(session).join('\n');
    // Should show rounded '1' rather than '0.67'; raw value still in JSON via toJSON.
    expect(md).toContain('border: 1 / 1 / 1 / 1');
    expect(md).not.toContain('0.67');
  });
});
```

*   **Step 2: Run test to verify it fails**

Run: `pnpm -F @tw199501/specsnap-core exec vitest run src/serialize-md.test.ts`
Expected: new test FAILS with a message about `0.67` appearing in output.

*   **Step 3: Implement display rounding helper + use it for border**

In `packages/core/src/serialize-md.ts`, find the line:

```
    `- border: ${bd[0]} / ${bd[1]} / ${bd[2]} / ${bd[3]}${a('border')}`,
```

Replace with:

```
    `- border: ${displayPx(bd[0])} / ${displayPx(bd[1])} / ${displayPx(bd[2])} / ${displayPx(bd[3])}${a('border')}`,
```

At the bottom of `packages/core/src/serialize-md.ts` (after the last function), add:

```
/**
 * Display helper for potentially-subpixel values.
 * Fractional widths like 0.67 (from a DPR 1.5 screen rendering a 1 CSS px
 * border) read as noise in a human-facing MD. Round to the nearest integer
 * for display but only when the original is within 0.1 of an integer — so
 * a truly intentional 0.5 line still prints as 0.5.
 */
function displayPx(v: number): string {
  const rounded = Math.round(v);
  if (Math.abs(v - rounded) < 0.1) return String(rounded);
  return String(v);
}
```

*   **Step 4: Run tests to verify they pass**

Run: `pnpm -F @tw199501/specsnap-core test`
Expected: all tests PASS (74 existing + 1 new = 75).

*   **Step 5: Commit**

```
git add packages/core/src/serialize-md.ts packages/core/src/serialize-md.test.ts
git commit -m "feat(core): round near-integer subpixel border widths in MD display"
```

---

### Task 3: FSA adapter scaffold + feature detection

Context: `File System Access API` (FSA) lets the playground write files to a user-picked folder instead of dumping everything into `Downloads/`. Chrome/Edge 86+ support it; Firefox/Safari partially or not at all. Build the adapter so the UI only talks to one `writeBundle()` function and never sees FSA vs download directly.

**Files:**

Create: `apps/playground/fs-access.ts`

Create: `apps/playground/fs-access.test.ts`

 **Step 1: Write the failing test**

Create `apps/playground/fs-access.test.ts`:

```
import { describe, expect, it, vi } from 'vitest';

import { isFileSystemAccessSupported, mockShowDirectoryPicker } from './fs-access.js';

describe('isFileSystemAccessSupported', () => {
  it('returns false when window.showDirectoryPicker is undefined', () => {
    expect(isFileSystemAccessSupported({} as Window)).toBe(false);
  });

  it('returns true when window.showDirectoryPicker is a function', () => {
    const fakeWindow = { showDirectoryPicker: () => Promise.resolve({}) } as unknown as Window;
    expect(isFileSystemAccessSupported(fakeWindow)).toBe(true);
  });
});

describe('mockShowDirectoryPicker (test helper)', () => {
  it('resolves to the stub handle we inject', async () => {
    const stub = { name: 'fake-root' };
    const fakeWindow = { showDirectoryPicker: mockShowDirectoryPicker(stub) } as unknown as Window;
    expect(isFileSystemAccessSupported(fakeWindow)).toBe(true);
  });
});
```

*   **Step 2: Run test to verify it fails**

Run: `pnpm -F specsnap-playground exec vitest run fs-access.test.ts 2>&1 | tail -5` (if playground has no vitest configured, see step 3a).

Expected: FAIL (module doesn't exist) or no-test-runner-configured error.

*   **Step 2a: If playground has no vitest, add a minimal config**

Create `apps/playground/vitest.config.ts`:

```
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    environment: 'happy-dom',
    include: ['**/*.test.ts']
  }
});
```

Add to `apps/playground/package.json` scripts:

```
"test": "vitest run"
```

Add `vitest` + `happy-dom` to playground devDeps (matching core's versions):

```
"devDependencies": {
  "vite": "^8.0.0",
  "vitest": "^4.1.4",
  "happy-dom": "^20.9.0"
}
```

Run: `pnpm install`
Then: `pnpm -F specsnap-playground test 2>&1 | tail -5`
Expected: FAIL — module missing.

*   **Step 3: Write minimal implementation**

Create `apps/playground/fs-access.ts`:

```
/**
 * File System Access API adapter with a graceful fallback to `<a download>`.
 * The caller only sees one entry point (`writeBundle`) — this file hides
 * whether files ended up in a user-picked folder or the Downloads folder.
 */

export interface BundleFile {
  filename: string;
  blob: Blob;
}

export interface BundleToWrite {
  dirName: string;
  markdownFilename: string;
  markdownContent: string;
  images: readonly BundleFile[];
}

export type WriteMode = 'filesystem' | 'downloads';

export interface WriteResult {
  mode: WriteMode;
  /** Human-readable description of where files landed. */
  where: string;
  /** Count of files written. */
  fileCount: number;
}

/**
 * Feature detection. Pass `window` explicitly so tests can inject a stub.
 */
export function isFileSystemAccessSupported(win: Window): boolean {
  return typeof (win as unknown as { showDirectoryPicker?: unknown }).showDirectoryPicker === 'function';
}

/**
 * Build a stub for `window.showDirectoryPicker` that tests can inject
 * via `isFileSystemAccessSupported({ showDirectoryPicker: mockShowDirectoryPicker(handle) })`.
 */
export function mockShowDirectoryPicker(handle: unknown) {
  return () => Promise.resolve(handle);
}
```

*   **Step 4: Run test to verify it passes**

Run: `pnpm -F specsnap-playground test 2>&1 | tail -5`
Expected: 3 tests PASS.

*   **Step 5: Commit**

```
git add apps/playground/fs-access.ts apps/playground/fs-access.test.ts apps/playground/vitest.config.ts apps/playground/package.json pnpm-lock.yaml
git commit -m "feat(playground): scaffold fs-access adapter with feature detection"
```

---

### Task 4: FSA adapter — directory picker + IndexedDB persistence

Context: the adapter needs to cache the user's chosen root directory so they don't have to re-pick after every page reload. Use IndexedDB (only place where you can persist a `FileSystemDirectoryHandle`). Each `setupWriteTarget()` call checks for a cached handle, re-verifies permission, and only prompts if nothing is cached or permission was revoked.

**Files:**

Modify: `apps/playground/fs-access.ts`

Modify: `apps/playground/fs-access.test.ts`

 **Step 1: Write failing tests for IndexedDB layer**

Append to `apps/playground/fs-access.test.ts`:

```
describe('persisted root handle', () => {
  it('returns null when nothing is cached', async () => {
    const { loadCachedRootHandle } = await import('./fs-access.js');
    // happy-dom has IndexedDB; ensure store is empty.
    const result = await loadCachedRootHandle('specsnap-test-store-empty');
    expect(result).toBeNull();
  });

  it('returns the cached handle after saveCachedRootHandle', async () => {
    const { loadCachedRootHandle, saveCachedRootHandle } = await import('./fs-access.js');
    const fake = { name: 'my-folder' };
    await saveCachedRootHandle('specsnap-test-store-roundtrip', fake as unknown as FileSystemDirectoryHandle);
    const loaded = await loadCachedRootHandle('specsnap-test-store-roundtrip');
    expect((loaded as unknown as { name: string } | null)?.name).toBe('my-folder');
  });
});
```

*   **Step 2: Run tests to verify they fail**

Run: `pnpm -F specsnap-playground test 2>&1 | tail -10`
Expected: 2 new tests FAIL (functions not exported).

*   **Step 3: Implement the IndexedDB helpers**

Append to `apps/playground/fs-access.ts`:

```
const DB_VERSION = 1;
const STORE_NAME = 'handles';

function openDb(dbName: string): Promise<IDBDatabase> {
  return new Promise((resolve, reject) => {
    const req = indexedDB.open(dbName, DB_VERSION);
    req.onupgradeneeded = () => {
      req.result.createObjectStore(STORE_NAME);
    };
    req.onsuccess = () => resolve(req.result);
    req.onerror = () => reject(req.error ?? new Error('IndexedDB open failed'));
  });
}

export async function saveCachedRootHandle(
  dbName: string,
  handle: FileSystemDirectoryHandle
): Promise<void> {
  const db = await openDb(dbName);
  try {
    await new Promise<void>((resolve, reject) => {
      const tx = db.transaction(STORE_NAME, 'readwrite');
      tx.objectStore(STORE_NAME).put(handle, 'root');
      tx.oncomplete = () => resolve();
      tx.onerror = () => reject(tx.error ?? new Error('IndexedDB put failed'));
    });
  }
  finally {
    db.close();
  }
}

export async function loadCachedRootHandle(
  dbName: string
): Promise<FileSystemDirectoryHandle | null> {
  const db = await openDb(dbName);
  try {
    return await new Promise<FileSystemDirectoryHandle | null>((resolve, reject) => {
      const tx = db.transaction(STORE_NAME, 'readonly');
      const req = tx.objectStore(STORE_NAME).get('root');
      req.onsuccess = () => resolve((req.result as FileSystemDirectoryHandle | undefined) ?? null);
      req.onerror = () => reject(req.error ?? new Error('IndexedDB get failed'));
    });
  }
  finally {
    db.close();
  }
}
```

*   **Step 4: Run tests to verify they pass**

Run: `pnpm -F specsnap-playground test 2>&1 | tail -5`
Expected: all tests PASS (5 total in fs-access.test.ts).

*   **Step 5: Commit**

```
git add apps/playground/fs-access.ts apps/playground/fs-access.test.ts
git commit -m "feat(playground): persist FSA root handle in IndexedDB"
```

---

### Task 5: FSA adapter — `writeBundle` with FSA write + downloads fallback

Context: the exported `writeBundle(bundle)` function is the playground's single entry point. It tries the FSA path first; on any failure (unsupported, user cancelled picker, permission denied, write error) it falls back to the downloads path. The return value tells the UI which path was taken so it can show an accurate hint ("files saved to ~/Documents/specsnap/20260420/" vs "files downloaded to your Downloads folder").

**Files:**

Modify: `apps/playground/fs-access.ts`

 **Step 1: Implement** `**writeBundle**` **\+ helper functions**

Append to `apps/playground/fs-access.ts`:

```
const DB_NAME = 'specsnap-playground-fs';

/**
 * Ensure we have an active root directory handle with readwrite permission.
 * Returns null if FSA isn't supported, user cancelled, or permission was denied.
 */
async function ensureRootHandle(): Promise<FileSystemDirectoryHandle | null> {
  if (!isFileSystemAccessSupported(window)) return null;
  const cached = await loadCachedRootHandle(DB_NAME);
  if (cached) {
    const permission = await cached.queryPermission({ mode: 'readwrite' });
    if (permission === 'granted') return cached;
    const next = await cached.requestPermission({ mode: 'readwrite' });
    if (next === 'granted') return cached;
  }
  try {
    const picked = await window.showDirectoryPicker({ mode: 'readwrite' });
    await saveCachedRootHandle(DB_NAME, picked);
    return picked;
  }
  catch {
    return null;
  }
}

async function writeFile(
  dir: FileSystemDirectoryHandle,
  filename: string,
  data: Blob | string
): Promise<void> {
  const fileHandle = await dir.getFileHandle(filename, { create: true });
  const writable = await fileHandle.createWritable();
  if (typeof data === 'string') await writable.write(data);
  else await writable.write(data);
  await writable.close();
}

async function writeBundleToFilesystem(
  root: FileSystemDirectoryHandle,
  bundle: BundleToWrite
): Promise<WriteResult> {
  const subdir = await root.getDirectoryHandle(bundle.dirName, { create: true });
  await writeFile(subdir, bundle.markdownFilename, bundle.markdownContent);
  for (const img of bundle.images) {
    await writeFile(subdir, img.filename, img.blob);
  }
  return {
    mode: 'filesystem',
    where: `${root.name}/${bundle.dirName}/`,
    fileCount: bundle.images.length + 1
  };
}

function writeBundleViaDownloads(bundle: BundleToWrite): WriteResult {
  triggerDownload(
    bundle.markdownFilename,
    new Blob([bundle.markdownContent], { type: 'text/markdown' })
  );
  for (const img of bundle.images) {
    triggerDownload(img.filename, img.blob);
  }
  return {
    mode: 'downloads',
    where: `Downloads/ (drag into ${bundle.dirName}/ manually)`,
    fileCount: bundle.images.length + 1
  };
}

function triggerDownload(filename: string, blob: Blob): void {
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = filename;
  document.body.appendChild(a);
  a.click();
  a.remove();
  URL.revokeObjectURL(url);
}

/**
 * Write the bundle to the best available target. Tries the File System
 * Access API first; on any failure (unsupported browser, user cancelled
 * the picker, permission denied, write error) falls back to triggering
 * multiple `<a download>` into the user's Downloads folder.
 */
export async function writeBundle(bundle: BundleToWrite): Promise<WriteResult> {
  const root = await ensureRootHandle();
  if (!root) return writeBundleViaDownloads(bundle);
  try {
    return await writeBundleToFilesystem(root, bundle);
  }
  catch (err) {
    console.warn('[specsnap] FSA write failed, falling back to downloads:', err);
    return writeBundleViaDownloads(bundle);
  }
}

/**
 * Forget any cached root-folder handle. Next `writeBundle` call will
 * prompt the user to pick a folder again.
 */
export async function clearCachedRoot(): Promise<void> {
  const db = await openDb(DB_NAME);
  try {
    await new Promise<void>((resolve, reject) => {
      const tx = db.transaction(STORE_NAME, 'readwrite');
      tx.objectStore(STORE_NAME).delete('root');
      tx.oncomplete = () => resolve();
      tx.onerror = () => reject(tx.error ?? new Error('IndexedDB delete failed'));
    });
  }
  finally {
    db.close();
  }
}
```

*   **Step 2: Type-check**

Run: `pnpm -F @tw199501/specsnap-core check`
Expected: exits 0.

Note: the playground doesn't run `tsc` — Vite does transform-only. For stronger checking we'd add a tsconfig to the playground, but that's out of scope for this task.

*   **Step 3: Commit (no new unit tests — ensureRootHandle / writeBundleToFilesystem are browser-FSA-only, verified via Playwright in Task 7)**

```
git add apps/playground/fs-access.ts
git commit -m "feat(playground): writeBundle adapter with FSA path + downloads fallback"
```

---

### Task 6: Wire `writeBundle` into the playground UI

Context: `main.ts` currently builds the bundle, writes MD to clipboard, and calls `triggerDownload` for each file inline. Replace the inline downloads with a single call to `writeBundle()` from the adapter. Show the write mode + location in the hint line after copy.

**Files:**

Modify: `apps/playground/main.ts`

Modify: `apps/playground/index.html`

 **Step 1: Add "Change folder" button to HTML**

In `apps/playground/index.html`, find:

```html
      <button id="specsnap-copy" class="btn specsnap-copy-btn" disabled>Copy MD</button>
    </div>
```

Replace with:

```html
      <button id="specsnap-copy" class="btn specsnap-copy-btn" disabled>Copy MD</button>
    </div>

    <div class="specsnap-fs-row" id="specsnap-fs-row" style="display:none">
      <span id="specsnap-fs-status"></span>
      <button id="specsnap-change-folder" class="btn specsnap-clear-btn" type="button">Change folder</button>
    </div>
```

In the `<style>` block (just before `.specsnap-hint`), add:

```css
    .specsnap-fs-row {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 6px;
      padding: 4px 12px;
      font-size: 10px;
      color: #888;
      border-bottom: 1px solid rgba(0, 0, 0, 0.06);
      flex-shrink: 0;
    }
    #specsnap-fs-status { font-family: ui-monospace, Consolas, monospace; }
```

*   **Step 2: Replace inline** `**triggerDownload**` **calls with** `**writeBundle**` **\+ wire the Change-folder button**

In `apps/playground/main.ts`, add the import near the top alongside the other imports:

```
import { clearCachedRoot, writeBundle, type WriteResult } from './fs-access.js';
```

Add element references near the other `getElementById` block:

```
const fsRowEl = document.getElementById('specsnap-fs-row') as HTMLDivElement | null;
const fsStatusEl = document.getElementById('specsnap-fs-status') as HTMLSpanElement | null;
const changeFolderBtn = document.getElementById('specsnap-change-folder') as HTMLButtonElement | null;
```

Update the `if (...)` guard at top-of-file to include the three new elements:

```
if (
  !panel || !header || !closeBtn || !inspectBtn || !clearBtn || !copyBtn
  || !hintEl || !outputEl || !emptyEl || !boxmodelsEl
  || !jsonEl || !captureIdEl || !targetsEl
  || !fsRowEl || !fsStatusEl || !changeFolderBtn
) {
  throw new Error('playground: required elements missing');
}
```

Replace the existing `triggerDownload` block inside the copy button handler:

```
    triggerDownload(bundle.markdownFilename, new Blob([bundle.markdownContent], { type: 'text/markdown' }));
    for (const img of bundle.images) triggerDownload(img.filename, img.blob);
```

With:

```
    const writeResult: WriteResult = await writeBundle({
      dirName: bundle.dirName,
      markdownFilename: bundle.markdownFilename,
      markdownContent: bundle.markdownContent,
      images: bundle.images
    });
    showFsStatus(writeResult);
```

Delete the helper function `triggerDownload` (lines below `copyBtn.addEventListener`) from `main.ts` — it's now owned by `fs-access.ts`.

Add these two helpers near the bottom of the file (before the initial state calls):

```
function showFsStatus(result: WriteResult): void {
  fsRowEl!.style.display = 'flex';
  fsStatusEl!.textContent = result.mode === 'filesystem'
    ? `✓ Saved to ${result.where} (${result.fileCount} files)`
    : `↓ Downloaded to ${result.where} (${result.fileCount} files)`;
}

changeFolderBtn.addEventListener('click', async () => {
  await clearCachedRoot();
  fsStatusEl!.textContent = 'Folder cleared — next Copy MD will prompt again';
});
```

*   **Step 3: Verify type-check + tests still pass**

Run: `pnpm -F @tw199501/specsnap-core check`
Expected: exits 0.

Run: `pnpm test`
Expected: all tests PASS.

*   **Step 4: Commit**

```
git add apps/playground/main.ts apps/playground/index.html
git commit -m "feat(playground): Copy MD goes through writeBundle (FSA → downloads fallback)"
```

---

### Task 7: Rebuild core + Playwright verify FSA end-to-end

Context: Vite serves core from `packages/core/dist`, so any core change not yet in dist won't be picked up. Rebuild before Playwright. Then verify the FSA path works in a real Chromium.

**Files:**

None (verification only)

 **Step 1: Rebuild core**

Run: `pnpm -F @tw199501/specsnap-core build`
Expected: build success, dist regenerated.

*   **Step 2: Make sure playground dev server is running on 5999**

Run: `pnpm -F specsnap-playground dev`
Expected: prints `Local: http://localhost:5999/` (predev kill-port hook ensures this).

*   **Step 3: Navigate Playwright to playground**

Use `mcp__plugin_playwright_playwright__browser_navigate` with `http://localhost:5999`.

Confirm page loads with no console errors beyond the benign favicon 404.

*   **Step 4: Manual verification script**

Claude-as-executor: since Playwright's FSA directory picker requires headed interaction that the MCP driver can't answer, at this step HAND OFF to the human user with an explicit checklist:

> **User verification needed.** Open Chrome/Edge at http://localhost:5999:
>
> 1.  Click Start Inspect → Save button → username → Done.
> 2.  Click Copy MD.
> 3.  Expect a folder picker dialog. Pick a folder (e.g., `~/Documents/specsnap`).
> 4.  Verify: the `fs-row` shows `✓ Saved to <folder>/20260420/ (3 files)`.
> 5.  Open the folder in your OS file manager. Expect `20260420/20260420-01.md`, `20260420-01-1.png`, `20260420-01-2.png`.
> 6.  Click Copy MD again. Expect NO picker this time; files land in `20260420-02.*`.
> 7.  Click "Change folder". Click Copy MD. Expect the picker to appear again.
> 8.  In Firefox / Safari (if available): Copy MD should download files into Downloads folder; `fs-row` shows `↓ Downloaded ...`.

Wait for user confirmation all 8 checks passed.

*   **Step 5: Close Playwright**

Use `mcp__plugin_playwright_playwright__browser_close`.

---

### Task 8: Bump to 0.0.4 + update core README + publish

**Files:**

Modify: `packages/core/package.json`

Modify: `packages/core/README.md`

 **Step 1: Bump version**

In `packages/core/package.json`, change:

```
  "version": "0.0.3",
```

To:

```
  "version": "0.0.4",
```

*   **Step 2: Update README Status section**

In `packages/core/README.md`, find:

```
- `v0.0.3` **(current)** — `toAnnotatedPNG` per-frame PNG + `toSpecSnapBundle` disk-ready packaging + relative-path image refs in markdown

Roadmap:
- `v0.0.4` — consumer-agnostic filesystem write helpers (File System Access API wrapper, extension `chrome.downloads` helper); subpixel value display polish
```

Replace with:

```
- `v0.0.3` — `toAnnotatedPNG` per-frame PNG + `toSpecSnapBundle` disk-ready packaging + relative-path image refs in markdown
- `v0.0.4` **(current)** — subpixel display polish (border rounding); playground gains File System Access API adapter so Copy MD writes into a user-picked folder instead of Downloads/

Roadmap:
```

*   **Step 3: Full gate**

```
pnpm check   # LF + tsc
pnpm test    # 75 tests
pnpm build   # dist regenerate
```

All must exit 0.

*   **Step 4: Root README version bump**

In `README.md` (root), update the Packages table row:

```
| [`@tw199501/specsnap-core`](./packages/core) | ✅ 0.0.4 | TypeScript library: capture + serialize (MD / JSON) + annotated PNG + disk-ready bundles |
```

*   **Step 5: Commit + tag + publish**

```
git add packages/core/package.json packages/core/README.md README.md
git commit -m "release: @tw199501/specsnap-core@0.0.4"
git tag -a core@0.0.4 -m "core 0.0.4 — FSA adapter in playground + border subpixel display polish"
git push origin main
git push origin core@0.0.4

cd packages/core
npm publish --otp=<your OTP>
cd ../..
```

*   **Step 6: Verify on npm registry**

Run: `npm view @tw199501/specsnap-core@0.0.4 version`
Expected: `0.0.4`.

---

# PART 2 — v0.0.5 (Schema depth + close-out)

### Task 9: `data-i18n-key` + `data-v-source` reading in core

Context: Many apps have build-time injection of i18n keys (`data-i18n-key="general.save"`) and source locations (`data-v-source="Button.vue:42-67"`). Core reads these if present; if absent, the fields stay `undefined`. The matching Vite plugin that INJECTS the attributes is out of scope for today — 0.0.5 just reads.

**Files:**

Modify: `packages/core/src/types.ts`

Modify: `packages/core/src/capture.ts`

Modify: `packages/core/src/capture.test.ts`

 **Step 1: Write failing tests**

In `packages/core/src/capture.test.ts`, inside the existing `describe('captureElement', ...)` block, add:

```
  it('reads data-i18n-key attribute into identity.i18nKey when present', () => {
    clearBody();
    const el = document.createElement('button');
    el.setAttribute('data-i18n-key', 'general.save');
    el.textContent = 'Save';
    mount(el);
    const frame = captureElement(el, 1);
    expect(frame.identity.i18nKey).toBe('general.save');
  });

  it('leaves identity.i18nKey undefined when the attribute is absent', () => {
    clearBody();
    const el = mount(makeElement({ tag: 'button', text: 'Save' }));
    const frame = captureElement(el, 1);
    expect(frame.identity.i18nKey).toBeUndefined();
  });

  it('reads data-v-source attribute into identity.source when present', () => {
    clearBody();
    const el = document.createElement('button');
    el.setAttribute('data-v-source', 'Button.vue:42-67');
    mount(el);
    const frame = captureElement(el, 1);
    expect(frame.identity.source).toBe('Button.vue:42-67');
  });

  it('leaves identity.source undefined when the attribute is absent', () => {
    clearBody();
    const el = mount(makeElement({ tag: 'button', text: 'Save' }));
    const frame = captureElement(el, 1);
    expect(frame.identity.source).toBeUndefined();
  });
```

*   **Step 2: Run tests to verify they fail**

Run: `pnpm -F @tw199501/specsnap-core exec vitest run src/capture.test.ts`
Expected: 4 new tests FAIL (TypeScript error: property does not exist).

*   **Step 3: Extend** `**ElementIdentity**` **type**

In `packages/core/src/types.ts`, find:

```
export interface ElementIdentity {
  tagName: string;
  id: string | null;
  classList: readonly string[];
  /** Best-effort semantic name, e.g. 'Button[text="Save"]' or 'input#port'. */
  name: string;
  /** CSS selector path that uniquely locates the element. */
  domPath: string;
}
```

Replace with:

```
export interface ElementIdentity {
  tagName: string;
  id: string | null;
  classList: readonly string[];
  /** Best-effort semantic name, e.g. 'Button[text="Save"]' or 'input#port'. */
  name: string;
  /** CSS selector path that uniquely locates the element. */
  domPath: string;
  /**
   * i18n key, read from the `data-i18n-key` attribute if a build-time tool
   * (i18n-ally, a custom Vite plugin, etc.) injected it. Omitted when
   * the attribute is absent — core doesn't guess.
   */
  i18nKey?: string;
  /**
   * Source location, e.g. `Button.vue:42-67`, read from the `data-v-source`
   * attribute when present. Omitted when the attribute is absent —
   * the matching build-time plugin is shipped separately (roadmap).
   */
  source?: string;
}
```

Also bump `SCHEMA_VERSION`:

```
export const SCHEMA_VERSION = '0.0.5';
```

*   **Step 4: Read the attributes in** `**identify()**`

In `packages/core/src/capture.ts`, find:

```
function identify(el: Element): ElementIdentity {
  const tagName = el.tagName.toLowerCase();
  const id = el.id || null;
  const classList = [...el.classList];
  const name = formatName(tagName, id, classList, el.textContent);
  const domPath = buildDomPath(el);
  return { tagName, id, classList, name, domPath };
}
```

Replace with:

```
function identify(el: Element): ElementIdentity {
  const tagName = el.tagName.toLowerCase();
  const id = el.id || null;
  const classList = [...el.classList];
  const name = formatName(tagName, id, classList, el.textContent);
  const domPath = buildDomPath(el);
  const identity: ElementIdentity = { tagName, id, classList, name, domPath };
  const i18nKey = el.getAttribute('data-i18n-key');
  if (i18nKey) identity.i18nKey = i18nKey;
  const source = el.getAttribute('data-v-source');
  if (source) identity.source = source;
  return identity;
}
```

*   **Step 5: Fix the existing SCHEMA\_VERSION assertion in** `**capture.test.ts**`

In `packages/core/src/capture.test.ts`, find:

```
    expect(session.schemaVersion).toBe('0.0.2');
```

Replace with:

```
    expect(session.schemaVersion).toBe('0.0.5');
```

(There may be multiple occurrences — update them all.)

*   **Step 6: Run tests to verify they pass**

Run: `pnpm -F @tw199501/specsnap-core test`
Expected: all tests PASS (75 + 4 = 79).

*   **Step 7: Commit**

```
git add packages/core/src/types.ts packages/core/src/capture.ts packages/core/src/capture.test.ts
git commit -m "feat(core): identity reads data-i18n-key + data-v-source when present"
```

---

### Task 10: Emit new identity fields in the Markdown body

Context: having them in JSON isn't enough — AI reading the MD should see `i18nKey` and `source` in the "基本 (Basics)" section so it can do i18n lookups / open the right source file.

**Files:**

Modify: `packages/core/src/serialize-md.ts`

Modify: `packages/core/src/serialize-md.test.ts`

 **Step 1: Write the failing tests**

In `packages/core/src/serialize-md.test.ts`, append:

```
describe('identity extras in MD body', () => {
  it('emits i18n_key line in Basics when identity.i18nKey is set', () => {
    clearBody();
    const el = document.createElement('button');
    el.setAttribute('data-i18n-key', 'general.save');
    el.textContent = 'Save';
    mount(el);
    const session = captureSession([el]);
    const md = toMarkdown(session)[0]!;
    expect(md).toContain('**i18n_key**: `general.save`');
  });

  it('emits source line in Basics when identity.source is set', () => {
    clearBody();
    const el = document.createElement('button');
    el.setAttribute('data-v-source', 'Button.vue:42-67');
    mount(el);
    const session = captureSession([el]);
    const md = toMarkdown(session)[0]!;
    expect(md).toContain('**source**: `Button.vue:42-67`');
  });

  it('omits both lines when neither attribute is set', () => {
    clearBody();
    const el = mount(makeElement({ tag: 'button', text: 'Save' }));
    const session = captureSession([el]);
    const md = toMarkdown(session)[0]!;
    expect(md).not.toContain('**i18n_key**');
    expect(md).not.toContain('**source**');
  });
});
```

Make sure the `import` block at the top of the test file includes `captureSession`, `clearBody`, `makeElement`, `mount` (add what's missing).

*   **Step 2: Run tests to verify they fail**

Run: `pnpm -F @tw199501/specsnap-core exec vitest run src/serialize-md.test.ts`
Expected: 3 new tests FAIL.

*   **Step 3: Emit the lines in the Basics section**

In `packages/core/src/serialize-md.ts`, find:

```
    '## 基本 (Basics)',
    `- **name**: \`${identity.name}\``,
    `- **dom_path**: \`${identity.domPath}\``,
    `- **position**: (${rect.x}, ${rect.y}) · viewport-relative (${viewportRelative.xPct}%, ${viewportRelative.yPct}%)`,
```

Replace with:

```
    '## 基本 (Basics)',
    `- **name**: \`${identity.name}\``,
    ...(identity.i18nKey ? [`- **i18n_key**: \`${identity.i18nKey}\``] : []),
    ...(identity.source ? [`- **source**: \`${identity.source}\``] : []),
    `- **dom_path**: \`${identity.domPath}\``,
    `- **position**: (${rect.x}, ${rect.y}) · viewport-relative (${viewportRelative.xPct}%, ${viewportRelative.yPct}%)`,
```

*   **Step 4: Run tests to verify they pass**

Run: `pnpm -F @tw199501/specsnap-core test`
Expected: all tests PASS (82 total).

*   **Step 5: Commit**

```
git add packages/core/src/serialize-md.ts packages/core/src/serialize-md.test.ts
git commit -m "feat(core): emit i18n_key and source lines in MD Basics when present"
```

---

### Task 11: Draft `publish.yml` workflow (inactive until NPM\_TOKEN is set)

Context: Manually running `npm publish --otp=XXX` is fine for now, but a tag-triggered workflow makes 0.0.6+ effortless. The workflow file can sit in the repo even before `NPM_TOKEN` secret is set — it'll just fail loudly on the publish step until the user adds the secret.

**Files:**

Create: `.github/workflows/publish.yml`

 **Step 1: Write the workflow**

Create `.github/workflows/publish.yml`:

```
name: Publish core to npm

on:
  push:
    tags:
      - 'core@*'

# Only one publish run at a time per tag, no cancelling — publishes must finish.
concurrency:
  group: publish-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    name: Publish ${{ github.ref_name }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write   # to attach the GitHub Release
      id-token: write   # for npm provenance (optional but nice)
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.0
          run_install: false

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Full gate (check + test + build)
        run: pnpm check && pnpm test && pnpm build

      - name: Verify tag matches package version
        working-directory: packages/core
        run: |
          TAG="${GITHUB_REF_NAME#core@}"
          PKG=$(node -p "require('./package.json').version")
          if [ "$TAG" != "$PKG" ]; then
            echo "Tag $TAG does not match package version $PKG" >&2
            exit 1
          fi
          echo "Tag $TAG matches package version $PKG"

      - name: Publish to npm
        working-directory: packages/core
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish --access public --provenance

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ github.ref_name }}
          generate_release_notes: true
          fail_on_unmatched_files: false
```

*   **Step 2: Verify YAML lint**

Run: `pnpm check:eol` (make sure LF)
Expected: `All N tracked text files use LF.`

(There's no YAML lint configured — the workflow will be validated by GitHub on push.)

*   **Step 3: Commit**

```
git add .github/workflows/publish.yml
git commit -m "ci: add tag-triggered npm publish workflow (requires NPM_TOKEN secret)"
```

*   **Step 4: Document what the user needs to do**

At this point, remind the user:

> To activate automated publishing for v0.0.6+:
>
> 1.  Generate an **Automation** token at https://www.npmjs.com/settings/tw199501/tokens (this kind skips 2FA).
> 2.  Add it as a repo secret named `NPM_TOKEN` at https://github.com/TW199501/specsnap/settings/secrets/actions/new
>
> Until that's set, pushing a `core@*` tag will fail at the "Publish to npm" step — that's expected and harmless.

---

### Task 12: Release v0.0.5 (manual publish — workflow sits dormant until token)

**Files:**

Modify: `packages/core/package.json`

Modify: `packages/core/README.md`

Modify: `README.md` (repo root)

 **Step 1: Bump version**

In `packages/core/package.json`, change:

```
  "version": "0.0.4",
```

To:

```
  "version": "0.0.5",
```

*   **Step 2: Update core README**

In `packages/core/README.md`, update Status section:

```
- `v0.0.4` — File System Access API adapter in playground; subpixel display polish
- `v0.0.5` **(current)** — `data-i18n-key` + `data-v-source` attribute reading into `ElementIdentity.i18nKey` / `.source`. SCHEMA_VERSION bumps to `'0.0.5'`.

Roadmap:
- `v0.1.0+` — companion Vite plugin `@tw199501/specsnap-vite-plugin` that auto-injects `data-v-source` + `data-i18n-key` at build time. Pseudo-state capture, recursive children dump.
```

Also in core README, under "What you get per frame" section, update the Identity bullet:

```
- **Identity** — `tagName`, `id`, `classList`, semantic `name`, unique `domPath`, optional `i18nKey` (when `data-i18n-key` is present), optional `source` (when `data-v-source` is present)
```

*   **Step 3: Update root README**

In `README.md` (root), update the Packages table row version column to `✅ 0.0.5` and add a brief "What v0.0.5 brought" sentence alongside the v0.0.3 one.

*   **Step 4: Full gate + build**

```
pnpm check
pnpm test
pnpm build
```

All must exit 0.

*   **Step 5: Commit + tag + push + publish**

```
git add packages/core/package.json packages/core/README.md README.md
git commit -m "release: @tw199501/specsnap-core@0.0.5"
git tag -a core@0.0.5 -m "core 0.0.5 — data-i18n-key + data-v-source identity fields"
git push origin main
git push origin core@0.0.5

cd packages/core
npm publish --otp=<your OTP>
cd ../..
```

*   **Step 6: If user has set NPM\_TOKEN, verify publish.yml also ran**

Visit https://github.com/TW199501/specsnap/actions — if `Publish core to npm` workflow succeeded for `core@0.0.5` tag, the automation is live for v0.0.6+.

If workflow failed at "Publish to npm" step — that's expected if `NPM_TOKEN` isn't set; the manual `npm publish` above already covered it.

*   **Step 7: Verify on registry**

Run: `npm view @tw199501/specsnap-core version`
Expected: `0.0.5`.

---

### Task 13: Write the 0.0.x closeout retrospective

Context: Document what shipped, what was learned, what's deferred. Lives alongside the earlier retrospective for continuity.

**Files:**

Create: `docs/superpower/plan/2026-04-20-retrospective-v003-to-v005.md`

 **Step 1: Write the document**

Create `docs/superpower/plan/2026-04-20-retrospective-v003-to-v005.md`:

```
# SpecSnap Core 0.0.3 → 0.0.5 — Closeout Retrospective

**Shipped:** 2026-04-20 (three releases in one day, same dev session)
- `@tw199501/specsnap-core@0.0.3` — toAnnotatedPNG + toSpecSnapBundle + relative-path image refs
- `@tw199501/specsnap-core@0.0.4` — File System Access API adapter, subpixel display polish
- `@tw199501/specsnap-core@0.0.5` — data-i18n-key + data-v-source identity fields, SCHEMA_VERSION 0.0.5

**Repo:** https://github.com/TW199501/specsnap (now public)

## Headline numbers

| Metric | v0.0.1 (prior release) | v0.0.5 (now) |
| --- | --- | --- |
| Tarball packed | 15.7 KB | ~60 KB |
| Tarball unpacked | 83 KB | ~210 KB |
| Runtime deps | 0 | 1 (dom-to-image-more, dynamic-imported) |
| Tests | 36 | 82 |
| Public APIs | captureSession, toMarkdown, toJSON, annotate | + captureElement, toAnnotatedPNG, toSpecSnapBundle, buildAnnotationSvg, formatCaptureId, formatDateYYYYMMDD |
| Schema fields | Session/Frame/Gap | + optional ElementIdentity.i18nKey, .source |

## Three architectural turns mid-session

1. **`toAnnotatedMarkdown` (base64-embedded) → separate MD + PNG files.** First attempt embedded data URLs inline; paste test produced ~48 KB of unreadable base64 per 3-frame session. Reverted in the same session; replaced by relative-path refs + independent PNG blobs.
2. **PNG thumbnail in the panel → box-model diagram in the panel.** First attempt rendered the screenshot thumbnail inline for verification; user clarified they wanted the schematic box model (margin/border/padding/content rectangles with edge numbers) instead. PNG rendering dropped from the live UI; only produced by Copy MD.
3. **`<a download>` → File System Access API with fallback.** Files previously drifted to `Downloads/`; users had to move them. FSA adapter + IndexedDB-persisted handle means Chrome/Edge users pick a folder once and all subsequent Copy MD calls land in `<folder>/YYYYMMDD/`. Firefox/Safari keep the fallback.

## What the discipline produced

- **TDD for every capture/serialize function.** RED → GREEN → COMMIT for every schema change. Bugs surfaced at the test expectation, not downstream.
- **74 → 82 tests across the three releases.** Every new API has at least one happy-path test and one "absent" test (for optional fields).
- **LF enforcement across 56 tracked files.** Never a CRLF scare in 20+ commits on Windows.
- **Playwright self-test.** Caught the "live overlay pollutes the PNG" bug in v0.0.3, would never have surfaced from unit tests alone.

## What would be different next time

- **Ask for the real output target sooner.** 0.0.3 went through three UX pivots on "where does the visual verification live" — first Inline thumbnails, then box-model, then FSA-on-disk. Each pivot was driven by user seeing the output. A brainstorming step asking "what does the consumer physically DO with this" before any code would have collapsed those pivots.
- **Formal tests for the playground.** The playground had no unit tests until 0.0.4 Task 3 added the fs-access harness. That meant three pivots of the main.ts were verified only by Playwright — slow, manual. A test suite on the orchestration logic would have caught at least one bug earlier.

## Shipped vs vision principles

| Vision principle | 0.0.5 status |
| --- | --- |
| P1 — Viewport is sacred | ✅ Session always carries viewport, rect is document-relative, viewport-relative percentages computed separately |
| P2 — Batch, not barrage | ✅ N selections → one Session, one Copy → one bundle with N frames |
| P3 — Bilingual default | ✅ 56-prop lexicon still; MD labels paired inline (`120 (寬度) × 34 (高度) px`) |
| P4 — Two views of one truth | ✅ MD text + annotated PNG share the same session, same filenames, relative-path refs inside MD |
| P5 — Recursive + semantic filter | ❌ Deferred to 0.1.0 — needs the Vite plugin for source-location injection before the recursion heuristic is well-defined |
| P6 — Conversation-ready | ✅ MD is direct-paste into Claude/ChatGPT/Cursor; PNGs drop in as attachments |
| P7 — Observe, not modify | ✅ No write APIs to the host page; overlay rendering injects briefly during capture only |

**6 of 7 principles live in 0.0.5; 1 on the 0.1.0 roadmap.**

## Next chapter (0.1.0+)

- `@tw199501/specsnap-vite-plugin` — build-time injection of `data-v-source` and `data-i18n-key` so the reverse-lookup fields populate automatically in Vue/SFC apps.
- Pseudo-state capture — `:hover`, `:focus`, `:disabled` via `getComputedStyle(el, pseudo)` where supported; graceful fallback where not.
- Recursive children with semantic filter — walk subtrees, filter out no-op wrappers.
- Chrome / Edge / Firefox browser extension wrapping core.
```

*   **Step 2: LF check + commit**

```
pnpm check:eol
git add docs/superpower/plan/2026-04-20-retrospective-v003-to-v005.md
git commit -m "docs: 0.0.3→0.0.5 closeout retrospective"
```

---

## Self-Review

**1\. Spec coverage:**

*   FSA adapter (Task 2-6): ✅
*   `data-i18n-key` + `data-v-source` reading (Task 9-10): ✅
*   Border subpixel polish (Task 2): ✅
*   Root README refresh (Task 1, Task 8 Step 4, Task 12 Step 3): ✅
*   Publish workflow (Task 11): ✅
*   Version bumps + publishing (Tasks 8, 12): ✅
*   Retrospective (Task 13): ✅

No gaps.

**2\. Placeholder scan:**
No "TBD" / "later" / "fill in" / "similar to" patterns. Every step has complete code or exact commands.

**3\. Type consistency:**

*   `BundleToWrite`, `WriteResult`, `WriteMode` defined in Task 3, used in Task 5-6. Consistent.
*   `saveCachedRootHandle` / `loadCachedRootHandle` defined Task 4, called from `ensureRootHandle` / `clearCachedRoot` in Task 5. Consistent.
*   `ElementIdentity.i18nKey` / `.source` added Task 9, read by Task 10 emission. Consistent.
*   `SCHEMA_VERSION = '0.0.5'` set in Task 9; asserted matching by Task 9 Step 5.

Plan is coherent.
